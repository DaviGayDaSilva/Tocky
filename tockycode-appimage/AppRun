#!/bin/sh
# TockyCode AppImage Wrapper
# Professional AI Code Generator - 100% Free, Local

APPIMAGE_VERSION="1.0.0"
APPNAME="TockyCode"

# Get the directory where this script is located
APPDIR="$(cd "$(dirname "$0")" && pwd)"

# Export environment
export PATH="$APPDIR/usr/bin:$PATH"
export PYTHONPATH="$APPDIR/usr/lib/python3/dist-packages:$PYTHONPATH"

# Run the TockyCode CLI
exec python3 -c "
import sys
import os

# Add package to path
sys.path.insert(0, '$APPDIR/usr/lib/python3/dist-packages')

# Import and run main
from tockycode import main
sys.exit(main())
" "$@"